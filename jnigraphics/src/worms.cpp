#include <stdlib.h>
#include "worms.h"
void worms::test(const int xmax, const int ymax)
{
/*
  //addnewworm
  //if (random(100) > 70)
  if (wormstotal() < 2)
  {
    int r = random(256);
    int g = random(256);
    int b = random(256);
    int rgb = (r << 16) | (g << 8) | b;
    addWorm(random(xmax)/60, random(ymax)/60, rgb);
  }

  //addtoendofexistingworm
  int w = random(wormstotal());
  std::cout << w << std::endl;
  add(random(xmax)/60, random(ymax)/60, w);

  //addtoidxofexistingworm
  int w = random(wormstotal());
  //add(random(xmax)/60, random(ymax)/60, w, random(wormsize(w)));
  add(random(xmax)/60, random(ymax)/60, w, 3);

  std::cout << xys << std::endl;
  //xys.debugPrint();
*/
}

/*
int main()
{
  worms w;
  w.test(600, 500);
  std::cout << w << std::endl;
  return 0;
}
*/
